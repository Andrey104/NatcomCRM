import {Company} from '../company';

export class DealMount {
  id: number;
  actions: object;
  stages: object;
  comments: object;
  company: Company;
  date_mount: string;
  date: string;
  status: number;
  deal: number;
}
